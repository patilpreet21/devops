apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}
spec:
  selector:
    app: webpage
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80

